Репозиторий: nikozarserg-boop/Mary_me (ветка master). Задачи расставлены строго в порядке выполнения — выполняй их последовательно этапами. После выполнения пунктов из TODO.txt удаляй соответствующие строки из TODO.txt. Не проверяй ничего «в интернете» — вся необходимая техническая информация приведена в задачах ниже.

ЭТАП 1 — Быстрые баг-фиксы UI/логики

ЗАДАЧА 1.1 — Переименование слоя.
Файл: src/commonMain/kotlin/org/example/animation/ui/components/LayersPanel.kt, компонент CompactLayerItem (блок isEditing с BasicTextField, ~строки 169-197).
- Добавь в BasicTextField `singleLine = true`, `keyboardOptions = KeyboardOptions(imeAction = ImeAction.Done)` и `keyboardActions = KeyboardActions(onDone = { onConfirmRename() })`, чтобы Enter подтверждал переименование.
- Добавь `FocusRequester` и `Modifier.focusRequester(...).onFocusChanged { if (!it.isFocused && wasFocused) onConfirmRename() }`, а также `LaunchedEffect(Unit) { focusRequester.requestFocus() }` для автофокуса и подтверждения при потере фокуса.
- Обрабатывай Escape через `Modifier.onPreviewKeyEvent` для вызова onCancelRename.
- Удали пустой `LaunchedEffect(textFieldValue) { ... }`.
Импорты: androidx.compose.foundation.text.KeyboardActions, KeyboardOptions; androidx.compose.ui.text.input.ImeAction; androidx.compose.ui.focus.focusRequester, FocusRequester, onFocusChanged. engine.renameLayer уже корректен — движок не трогать.

ЗАДАЧА 1.2 — Ползунок масштаба интерфейса.
Файл: src/commonMain/kotlin/org/example/App.kt — в SettingsDialog передаётся uiScale = savedUiScale; в авто-режиме (savedUiScale <= 0f) слайдер получает 0, вне диапазона 0.5..2.0.
- Передавай uiScale = effectiveScale (или savedUiScale.takeIf { it > 0f } ?: autoScale).
Файл: src/commonMain/kotlin/org/example/animation/ui/components/SettingsPanel.kt, InterfaceSettingsContent (Slider, ~строки 246-251).
- value = scale.coerceIn(0.5f, 2.0f). Проверь, что onUiScaleChange сохраняет через AppSettingsManager.setUiScale и обновляет UI немедленно.

ЗАДАЧА 1.3 — Ручной ввод пути в проводнике.
Файл: src/commonMain/kotlin/org/example/animation/ui/components/FileManagerDialog.kt, блок «Навигация» (~строки 150-164): currentPath в неизменяемом Text.
- Замени на редактируемое поле (OutlinedTextField/BasicTextField), локальное состояние pathInput (инициализируй currentPath, синхронизируй через LaunchedEffect(currentPath)).
- По Enter (onDone) проверяй путь через fileHandler; если директория существует (fileHandler.fileExists(path) и это каталог) — currentPath = pathInput, иначе показывай errorMsg. entries пересчитывается через remember(currentPath).

ЗАДАЧА 1.4 — Названия во вкладке «Производительность».
Файлы: src/commonMain/composeResources/files/locales/strings_ru.json и strings_en.json.
- Вкладка (ключ settings.performance) содержит только настройки призрачных кадров (perf.ghostFrames, anim.ghostBefore/After/Desc/Color). Приведи подписи в соответствие содержимому в обоих языках, согласованно и грамотно. При добавлении новых ключей добавь их в оба json и используй в PerformanceSettingsContent (SettingsPanel.kt) через EditorStrings.observeString.

ЗАДАЧА 1.5 — Починить поле ввода FPS в панели таймлайна.
Файл: src/commonMain/kotlin/org/example/animation/ui/components/TimelinePanel.kt, поле ввода FPS в нижней панели управления (OutlinedTextField с fpsText, ~строки 266-285).
1. Рассинхрон диапазонов: поле валидирует `v in 1..240`, но engine.setFps (AnimationEngine.kt, ~строки 182-189) коэрсит в 1..120. Приведи диапазоны к единому значению (рекомендуется 120) во всех местах: TimelinePanel (валидация), AnimationEngine.setFps, SettingsPanel.onConfirm (~строки 160-163), NewProjectDialog, и строку локализации project.sizeError (в strings_ru.json/strings_en.json сейчас «FPS (1-240)» — привести к выбранному диапазону в обоих языках).
2. Сброс состояния во время набора: `var fpsText by remember(project.fps) { ... }` пересоздаёт состояние при каждом setFps. Убери привязку remember к project.fps — держи `var fpsText by remember { mutableStateOf(project.fps.toString()) }`; синхронизируй с внешним project.fps только когда поле НЕ в фокусе (onFocusChanged + LaunchedEffect(project.fps)).
3. Пустое/некорректное значение: разреши временно пустую строку и промежуточный ввод, но не вызывай setFps для невалидного значения. При потере фокуса и по Enter (onDone): если текст не парсится в валидный fps — верни fpsText к project.fps.toString(); если валиден — engine.setFps(v) и нормализуй текст.
- Добавь `keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Number, imeAction = ImeAction.Done)` и `keyboardActions = KeyboardActions(onDone = { ... коммит ... })`.
Импорты при необходимости: androidx.compose.foundation.text.KeyboardOptions, KeyboardActions; androidx.compose.ui.text.input.ImeAction, KeyboardType; androidx.compose.ui.focus.onFocusChanged.

ЭТАП 2 — Автосохранение и корректный выход

ЗАДАЧА 2.1 — Починить «Сохранить и выйти».
Файл: src/commonMain/kotlin/org/example/App.kt. В ConfirmExitDialog (~строки 212-221) кнопка onSaveAndExit ставит pendingFileAction = "save_all_exit", но LaunchedEffect(pendingFileAction) (~строки 70-88) обрабатывает только "save" и "save_and_close_tab" — действие "save_all_exit" не обрабатывается, поэтому кнопка ничего не делает.
- Добавь обработку "save_all_exit": пройди по всем projectManager.engines.value, для каждого с hasUnsavedChanges сохрани (если filePath.value != null — saveToPath + markAsSaved + AppSettingsManager.addRecentProject; если filePath.value == null — открой FileManagerDialog(SAVE) по очереди для проектов без пути). После сохранения всех вызови onExitConfirm().

ЗАДАЧА 2.2 — Диалог подтверждения выхода на десктопе.
Файл: src/jvmMain/kotlin/Main.kt (~строки 19-28). Сейчас Window onCloseRequest = { exitApplication() } закрывает приложение напрямую, минуя проверку несохранённых изменений (App() вызывается без параметров exitRequested/onExitConfirm).
- Проведи закрытие окна через флаг exitRequested и колбэки onExitConfirm/onExitCancel в App(), чтобы при наличии несохранённых изменений (LaunchedEffect(exitRequested), App.kt ~строки 133-138) показывался ConfirmExitDialog, а фактический exitApplication() вызывался только из onExitConfirm.

ЗАДАЧА 2.3 — Восстановление автосохранения при запуске.
Файлы: src/commonMain/kotlin/org/example/animation/engine/AnimationEngine.kt (startAutoSaveTimer ~143-153, performAutosave ~155-163, cleanup ~684-686), src/commonMain/kotlin/org/example/App.kt.
- Автосейв пишет в cacheDir/autosave_${id}.maryme, но нет ни восстановления при старте, ни удаления файла при нормальном закрытии — файлы накапливаются в кэше. Реализуй: при запуске проверяй наличие autosave-файлов в cacheDirectory; если есть — предложи восстановить (диалог) и открой восстановленный проект. При корректном сохранении/закрытии проекта удаляй его autosave-файл (в cleanup или markAsSaved). Учитывай, что id генерируется рандомно на каждый запуск (AnimationEngine.kt ~19-20) — восстановление ищи по маске autosave_*.maryme, а не по конкретному id.

ЭТАП 3 — Стартовый экран с недавними проектами

ЗАДАЧА 3.1 — Начальный экран приложения.
Сейчас App() сразу открывает EditorScreen (App.kt ~162-183). Сделай первый экран при запуске — экран выбора проектов (референс: стартовые экраны редакторов вроде After Effects / Krita / Blender). Создай новый Composable (например src/commonMain/kotlin/org/example/animation/ui/screens/StartScreen.kt).
Содержимое:
- Список/сетка недавних проектов из AppSettingsManager.getRecentProjects() (AppSettingsManager.kt ~89-105) — плашки с превью и именем/датой.
- Кнопки «Создать проект» (открывает NewProjectDialog, NewProjectDialog.kt ~23-27) и «Открыть проект» (FileManagerDialog OPEN).
- Кнопка/пункт «Открыть последний проект» — берёт первый элемент getRecentProjects() и открывает его сразу в редакторе.
- Красивый, простой, удобный интерфейс с анимациями кнопок/плашек (Compose animate* / AnimatedVisibility).
- После открытия/создания проекта переход в EditorScreen. Управляй экраном через состояние в App() (например var currentScreen: START/EDITOR).

ЗАДАЧА 3.2 — Анимированное превью проектов при наведении.
- На плашке недавнего проекта при наведении (desktop hover через Modifier.pointerMoveFilter/onPointerEvent; на Android — при появлении/долгом тапе) проигрывай анимацию проекта: рендери кадры проекта через Renderer.renderFrame в цикле по project.fps в ImageBitmap (НЕ декодируй видео — это наши .maryme проекты). Вне наведения показывай статичный первый кадр.
- Загружай проект для превью лениво (ProjectSerializer.deserialize из файла по пути), кэшируй отрендеренные кадры, следи за производительностью (не рендерить одновременно много плашек — ограничь активное превью одной наведённой плашкой).

ЭТАП 4 — Аудит корректности «чистого» кода

ЗАДАЧА 4.1 — Проверка и фикс потенциальных багов (обосновывай правки в коммитах):
- Самописный JSON-парсер в ProjectSerializer (src/commonMain/kotlin/org/example/animation/io/ProjectSerializer.kt): extractString/extractArray/splitTopLevel — граничные случаи: экранированные кавычки в именах, спецсимволы, вложенные массивы. Тесты из этапа 8 должны это выявить; при провале — чинить парсер, а не подгонять тест.
- Base64 encode/decode на длинах не кратных 3 и с паддингом (в т.ч. пустой массив).
- Устойчивость deserialize("") и deserialize("{}") — должен возвращать валидный проект хотя бы с одним слоем, не бросать исключение наружу.
- Синхронизация длины слоёв в moveFrame/moveFrameToLayer/pasteFrame (все слои одинаковой длины).
- Экспорт: команды FFmpeg продублированы в androidMain и jvmMain — держи их согласованными по кодекам/pix_fmt (см. этап 5).

ЭТАП 5 — Экспорт и встроенный FFmpeg (баг mp4/видео)

ЗАДАЧА 5.1 — Android: полноценный ffmpegkit + диагностика.
Файл: src/androidMain/kotlin/org/example/animation/io/PlatformFileHandlerImpl.kt (экспорт через FFmpegKit.execute(cmd), ~171-187), зависимость build.gradle.kts (~79: com.moizhassan.ffmpeg.ffmpegkit:ffmpegkit:6.1.1).
- Убедись, что подключённый вариант ffmpegkit содержит нужные энкодеры (libx264, libvpx-vp9, libaom, prores). Оригинальный com.arthenica ffmpegkit был снят с Maven Central в начале 2025; com.moizhassan — коммьюнити-реэрхост. Если текущий вариант min/lts без x264 — используй full-gpl вариант (проверь доступные артефакты реэрхоста; при недоступности full-gpl рассмотри альтернативный поддерживаемый форк). В рантайме проверь наличие энкодеров командой `-encoders` при инициализации и логируй результат.
- Диагностика ошибок: при неуспехе бери session.allLogsAsString и пробрасывай в exportError (сейчас показывается общий текст без причины).
- Фикс mov: pix_fmt yuva444p10le для prores_ks оставь, но проверь совместимость; при проблемах используй yuv420p/yuv422p10le.

ЗАДАЧА 5.2 — Десктоп: ВСТРОЕННЫЙ полноценный FFmpeg (без установки пользователем).
Файл: src/jvmMain/kotlin/org/example/animation/io/PlatformFileHandlerImpl.kt (~174-190: сейчас DefaultFFMPEGLocator().executablePath от JAVE — урезанный бинарник без libx264/libvpx/prores). НЕ использовать «системный ffmpeg из PATH» — FFmpeg должен быть встроен в дистрибутив.
- Замени зависимость JAVE (ws.schild:jave-all-deps) на JavaCPP presets FFmpeg GPL: `org.bytedeco:ffmpeg-platform-gpl:6.1.1-1.5.10` (содержит статические бинарники FFmpeg под Windows/Linux/macOS x86_64+arm64 с libx264/libx265/libvpx). Путь к встроенному бинарнику получай через `org.bytedeco.javacpp.Loader.load(org.bytedeco.ffmpeg.ffmpeg.class)` — он извлекает бинарник под текущую ОС в кэш и возвращает путь; установка не требуется.
- Захватывай stderr: вместо ProcessBuilder(cmd).inheritIO() перенаправляй потоки и читай вывод, чтобы в exportError попадала реальная причина.
- Проверь порядок аргументов ffmpeg: -framerate перед -i (входной fps), при необходимости -r после для выходного fps.
- Учти лицензию: GPL-сборка (x264/x265) накладывает GPL на приложение. Альтернатива для сохранения MIT — LGPL-сборка `org.bytedeco:ffmpeg-platform` (H.264 через libopenh264, без x264/x265). Реши согласованно с этапом 10 (лицензия/бейдж README). Реализуй выбранный вариант.

ЗАДАЧА 5.3 — Проверка всех форматов экспорта.
- Прогони реальный экспорт для каждого формата из ExportDialog.kt (~133: png, jpg, webp, gif, apng, mp4, webm, mov, mkv, avi) на Android и десктопе и убедись, что файл создаётся и валиден. Растровые (png/jpg/webp) — через ExportManager (App.kt ~300-331); остальное — через fileHandler.exportAnimation. Форматы, физически не поддерживаемые на платформе, скрой из ExportDialog для этой платформы либо покажи внятную ошибку.

ЗАДАЧА 5.4 — Общий кроссплатформенный хелпер предпросмотра видео (через встроенный FFmpeg).
- Добавь в интерфейс PlatformFileHandler (src/commonMain/kotlin/org/example/animation/io/PlatformIO.kt, ~54-69) метод извлечения кадров видеофайла в набор ImageBitmap/PNG через встроенный FFmpeg (десктоп — bytedeco, Android — ffmpegkit): например `extractVideoFrames(path, fps, maxFrames): List<ByteArray>` во временный каталог. Реализуй в обоих PlatformFileHandlerImpl. Это единый механизм превью «всех форматов, включая mp4», без сторонних плееров и без установки чего-либо пользователем.

ЗАДАЧА 5.5 — Предпросмотр в экране экспорта (до сохранения).
Файл: src/commonMain/kotlin/org/example/animation/ui/components/ExportDialog.kt.
- Добавь область предпросмотра, которая проигрывает анимацию проекта, рендеря кадры через Renderer.renderFrame в цикле по project.fps (это наш проект — декодирование видео не нужно). Для форматов-одиночных кадров (png/jpg/webp) показывай текущий кадр.

ЭТАП 6 — Окно результата экспорта (предпросмотр + поделиться + буфер)

ЗАДАЧА 6.1 — Платформенные методы для шаринга/буфера/открытия.
Файл: src/commonMain/kotlin/org/example/animation/io/PlatformIO.kt (интерфейс PlatformFileHandler ~54-69) — добавь методы: `shareFile(path)`, `copyFileToClipboard(path)` (openInExplorer уже есть).
- Android (src/androidMain/.../PlatformFileHandlerImpl.kt): shareFile через FileProvider.getUriForFile + Intent.ACTION_SEND (правильный MIME, FLAG_GRANT_READ_URI_PERMISSION, createChooser, FLAG_ACTIVITY_NEW_TASK при запуске не из Activity). Настрой FileProvider в AndroidManifest (authority ${applicationId}.fileprovider + res/xml/file_paths.xml). copyFileToClipboard — ClipboardManager + ClipData.newUri.
- Десктоп (src/jvmMain/.../PlatformFileHandlerImpl.kt): открытие — Desktop.getDesktop().open(file); буфер — Toolkit.getDefaultToolkit().systemClipboard с Transferable, отдающим DataFlavor.javaFileListFlavor (копирование самого файла).

ЗАДАЧА 6.2 — Окно результата после успешного экспорта.
Файл: src/commonMain/kotlin/org/example/App.kt (сейчас при успехе экспорта, ~329-334, просто сбрасывается прогресс; ошибка — в exportError). Создай новый Composable ExportResultDialog.
- Показывай после успешного экспорта. Содержимое: предпросмотр готового файла — для растровых показывай изображение (Image), для видео/анимаций (gif/apng/mp4/webm/mov/mkv/avi) проигрывай извлечённые через встроенный FFmpeg кадры (задача 5.4) в Compose-цикле по fps. Метаданные (формат, размер, путь).
- Кнопки: «Поделиться» (shareFile), «Копировать в буфер» (copyFileToClipboard), «Открыть» (открыть в системном приложении), «Показать в папке» (openInExplorer), «Закрыть».
- Строки кнопок/подписей добавь в strings_ru.json и strings_en.json.

ЭТАП 7 — Платформенные зависимости и форматы сборки

ЗАДАЧА 7.1 — Только нужные зависимости платформы в дистрибутиве.
Файл: build.gradle.kts (sourceSets ~58-93). Разделение уже есть (androidMain → ffmpegkit, jvmMain → десктопный FFmpeg). Для per-OS десктоп-сборок используй конкретные classifier-ы bytedeco (например windows-x86_64-gpl / linux-x86_64-gpl / macosx-arm64-gpl) вместо универсального -platform-gpl, чтобы в дистрибутив каждой ОС попадал только её бинарник (управляй через gradle-свойство -PtargetOs в CI, этап 9). Убедись, что FFmpeg-зависимость живёт только в jvmMain и не попадает в Android-сборку, а ffmpegkit — только в androidMain.

ЗАДАЧА 7.2 — Форматы дистрибутивов десктопа + AppImage.
Файл: build.gradle.kts, compose.desktop.application.nativeDistributions.targetFormats (~100-103: сейчас только Dmg и Exe).
- Добавь Msi (Windows), Deb и Rpm (Linux): `TargetFormat.Msi`, `.Deb`, `.Rpm`. Каждый формат собирается только на своей ОС (Deb/Rpm — Linux, Dmg — macOS, Exe/Msi — Windows) — это отражено в матрице CI.
- AppImage для Linux (Compose Desktop нативно не умеет AppImage) — отдельным шагом CI на ubuntu-latest:
  1. `./gradlew createReleaseDistributable` → build/compose/binaries/main-release/app/MaryMe/ (каталог с бинарником и bundled JRE).
  2. Сформируй AppDir: скопируй app/MaryMe в AppDir/usr/, создай AppRun (запуск bin/MaryMe), .desktop-файл (Name=MaryMe, Exec=MaryMe, Icon=maryme, Categories=Graphics;) и иконку maryme.png в корне AppDir.
  3. Скачай appimagetool (appimagetool-x86_64.AppImage из github.com/AppImage/appimagetool releases), сделай исполняемым, запусти `./appimagetool-x86_64.AppImage AppDir MaryMe-<version>.AppImage`. На headless-раннере используй `APPIMAGE_EXTRACT_AND_RUN=1` и/или установи libfuse2. Версию бери из version.txt.
  4. Проверь запуск AppImage (хотя бы `--appimage-extract`), загрузи артефакт (этап 9).

ЭТАП 8 — Юнит-тесты (kotlin.test)

ЗАДАЧА 8.1 — Настройка тестовых source set.
Файл: build.gradle.kts. Тестов сейчас нет. Заведи commonTest с зависимостью kotlin("test"); при необходимости jvmTest для тестов, требующих JVM-реализаций. Каталоги: src/commonTest/kotlin/... (платформонезависимые), src/jvmTest/kotlin/... .

ЗАДАЧА 8.2 — Покрытие (@Test, assertEquals/assertTrue/assertFailsWith):
1. Модель AnimationProject (src/commonMain/kotlin/org/example/animation/model/FrameData.kt ~159-217): ensureFrameCount; addLayer (кадров = totalFrames); removeLayer (не удаляет последний при size==1, удаляет при >1); moveLayer; totalFrames/maxFrames как максимум по слоям; duplicateFrame (копия после исходного + возврат индекса); removeFrame (не удаляет единственный кадр); copy() глубокая (независимость списков layers/frames/strokes); FrameData.isEmpty().
2. ProjectSerializer (commonTest): round-trip serialize→deserialize (имена, canvasWidth/Height, fps, dpi, backgroundColor, число слоёв/кадров/штрихов, координаты точек — float с допуском); serializeToBytes/deserializeFromBytes; base64 encode→decode (длины не кратные 3, пустой массив) на ImageElement.data; устойчивость deserialize("")/deserialize("{}").
3. AnimationEngine (jvmTest): addLayer/removeLayer + currentLayerIndex (не удаляет последний); renameLayer (пустая/пробельная не меняет, валидная обрезает пробелы); undo/redo + canUndo/canRedo; setFps коэрсит в выбранный диапазон (см. 1.5). Читай StateFlow через .value; playback (таймеры/Dispatchers) не тестируй.
Запуск: `./gradlew jvmTest` и `./gradlew testDebugUnitTest` проходят локально; подключены к CI (этап 9).

ЭТАП 9 — CI/CD (GitHub Actions)

Общее для всех workflow: actions/checkout@v4; actions/setup-java@v4 (temurin JDK 21); gradle/actions/setup-gradle@v4 (кеш); запуск через ./gradlew. Каталог .github/workflows/.

ЗАДАЧА 9.1 — CI (.github/workflows/ci.yml).
- Триггеры: push и pull_request в master.
- Job на ubuntu-latest: `./gradlew assembleDebug` (Android) и `./gradlew jvmJar` (десктоп-код) + тесты `./gradlew jvmTest testDebugUnitTest` (из этапа 8).

ЗАДАЧА 9.2 — Release (.github/workflows/release.yml).
- Триггеры: push тега `v*` и workflow_dispatch.
- Матрица десктопа (compose собирает нативный пакет только под текущую ОС), с -PtargetOs (задача 7.1):
  - windows-latest → Exe + Msi.
  - macos-latest → Dmg.
  - ubuntu-latest → Deb + Rpm + AppImage (задача 7.2).
  - Артефакты: build/compose/binaries/main-release/ → actions/upload-artifact@v4.
- Job Android (ubuntu-latest): `./gradlew assembleRelease bundleRelease` (APK + AAB).
  - ПОДПИСЬ: в android { } (build.gradle.kts ~110-143) НЕТ signingConfigs/buildTypes — assembleRelease даст неподписанный APK. Добавь signingConfigs(release) и buildTypes.release.signingConfig, читая storeFile/storePassword/keyAlias/keyPassword из env/gradle-свойств. В workflow восстанавливай keystore из secrets: RELE
